<Page x:Class="WpfDem.Pages.ProductsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfDem.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ProductsPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
            <TextBlock x:Name="UserNameText"/>
        </StackPanel>

        <ListView x:Name="ProductsListView" Grid.Row="2" HorizontalContentAlignment="Stretch">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Count}" Value="0">
                            <Setter Property="Background" Value="LightBlue"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding IsBigDiscount}" Value="True">
                            <Setter Property="Background" Value="#2E8B57"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Black" BorderThickness="1">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="120"/>
                            </Grid.ColumnDefinitions>
                            <Border BorderBrush="Black" BorderThickness="1"  Margin="5" Padding="5">
                                <Image Source="{Binding PhotoPath}" Stretch="Uniform"/>
                            </Border>
                            <StackPanel Grid.Column="1"  Margin="5" >
                                <TextBlock FontWeight="Bold">
                                    <Run Text="{Binding Category.Name}"/>
                                    <Run Text=" | "/>
                                    <Run Text="{Binding Name}"/>
                                </TextBlock>
                                <TextBlock Text="Описание товара:"/>
                                <TextBlock Text="{Binding Description}" 
                                           TextWrapping="Wrap"/>
                                <TextBlock>
                                    <Run Text="Производитель: "/>
                                    <Run Text="{Binding Manufacturer.Name}"/>
                                </TextBlock>
                                <TextBlock>
                                    <Run Text="Поставщик: "/>
                                    <Run Text="{Binding Supplier.Name}"/>
                                </TextBlock>
                                <TextBlock>
                                    <Run Text="Цена: "/>
                                    <Run Text="{Binding Price, StringFormat=C}" 
                                         Foreground="Red" 
                                         TextDecorations="Strikethrough"/>
                                    <Run Text="{Binding DiscountedPrice, StringFormat=C, Mode=OneWay}"/>
                                </TextBlock>
                                <TextBlock>
                                    <Run Text="Единица измерения: "/>
                                    <Run Text="{Binding Unit}"/>
                                </TextBlock>
                                <TextBlock>
                                    <Run Text="Количество на складе: "/>
                                    <Run Text="{Binding Count}"/>
                                </TextBlock>
                            </StackPanel>
                            <Border Grid.Column="2" BorderBrush="Black" BorderThickness="1" Margin="5" Padding="5">
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="Действующая" HorizontalAlignment="Center"/>
                                    <TextBlock Text="скидка" HorizontalAlignment="Center"/>
                                    <TextBlock FontSize="16" FontWeight="Bold" HorizontalAlignment="Center">
                                        <Run Text="{Binding Discount}"/>
                                        <Run Text="%"/>
                                    </TextBlock>
                                </StackPanel>
                            </Border>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
